---
"@ofocus/gateway": minor
---

New `@ofocus/gateway` package: a remote-MCP gateway exposing the OmniFocus MCP server to claude.ai and ChatGPT custom connectors over Streamable HTTP with OAuth 2.1 (Google-delegated login, single-email allowlist, DCR + PKCE + refresh tokens), with a config-driven tool-exposure gate.
